# 工作歷程

## 2026-09-23

- 使用者選定精簡版概念圖為新版 viewer UX 基準，原圖與補充規格集中於 [docs/ux/README.md](docs/ux/README.md)。Header 右側顯示已開啟檔名是原圖之外的已確認要求；本輪只記錄設計，尚未改造介面。
- 後續依定稿圖實作精簡版面：白色背景、淡網格、模型置中、精簡側欄與 Header 檔名；本機選檔時自動處理內嵌材質及同批選取的 USDA 覆蓋。
- 使用者再以 Blender 截圖確認視角旋鈕應是六個小球：X／Y／Z 實心、負軸空心；據此移除原先大球底座，改為隨鏡頭轉動的六軸端點。Docker 建置及瀏覽器互動驗證通過，公開網站尚未更新。
- 收尾檢查補上無 UV 網格的顯示警告；即使 USDA 材質節點已成功對應，也不能把貼圖色彩備援誤報為完整貼圖顯示。Docker Chrome 從真正的「載入模型」按鈕開啟檔案選擇器，同時載入最小 GLTF 與 USDA，確認警告可見。
- 使用者定案 Focoria 通用場景清單：新選檔批次取代舊批次，主模型形成清單，相依素材不列入。需求與驗收邊界記於 [Issue #5 規格](docs/spec/issue-5-user-selected-scenes-and-materials.md)。移除部署端 `manifest.json` 清單及沙崙專用 Vite 路由；一般模型內材質與同批相依檔自動載入，缺件和純色備援分開提示。保留唯一配對的 USDA 材質覆蓋相容功能。
- Docker 建置與 Puppeteer 瀏覽器測試通過；測試包含真實選檔、GLB、多模型切換、批次取代、`.blend` 拒絕、外部貼圖、缺件與 USDA 配對。停止舊 `jinher-shalun-l0-viewer` 和短期驗證容器後，以不掛載場景的 `focoria-local` 在 `127.0.0.1:5174` 重部署；同一套瀏覽器測試對該網址通過。GitHub Pages 未發布，程式碼尚未提交或推送。

## 2026-09-21

- 勘誤：僅停用無 UV 網格的貼圖不足以解決黑面。以 Docker 內 Chrome 載入完整沙崙 L1 GLB，再套用 L1-nomat USDA 重現問題。
- 全部 14,653 個網格缺少法線。原始材質採平面著色且雙面顯示，替換材質曾丟失這兩項設定；現在保留原始面向，並對缺法線網格啟用平面著色。
- 修正後完整場景截圖不再大片發黑；3,254 個節點套用 31 種材質，其中 2,724 個網格缺 UV，仍使用色彩備援。未宣稱已補齊貼圖座標或支援完整 USD 組合。
- Chrome 驗證為真實檔案選擇及套用按鈕流程；測試時暫停連續動畫、手動繪製截圖以降低軟體繪圖負載，正式程式不受此測試注入影響。建置通過，只有既有大型分塊警告。
